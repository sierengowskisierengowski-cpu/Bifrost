# Standalone project root.
#
# This file marks bifrost-desktop as its own pnpm workspace root so `pnpm install`
# resolves only this project's package.json, even if this directory is nested
# inside a larger monorepo with unrelated workspace dependencies.
packages: []
allowBuilds:
  esbuild: false
